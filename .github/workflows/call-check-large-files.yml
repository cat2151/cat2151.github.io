name: Call Check Large Files

on:
  schedule:
    # 日本時間 03:00 (UTC 18:00)
    - cron: '0 18 * * *'
  workflow_dispatch:

permissions:
  contents: read
  issues: write

jobs:
  call-check-large-files:
    uses: ./.github/workflows/check-large-files-reusable.yml
